spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/CsvFile?createDatabaseIfNotExist=true&autoReconnect=true
    username: postgres
    password: root
  jpa:
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
    hibernate:
      ddl-auto: create-drop
    show-sql: true

server:
  port: 8080
